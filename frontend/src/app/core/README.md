# Core

Infraestructura transversal del frontend: interceptores HTTP (auth, manejo de errores), guards de ruta y servicios singleton (ej. `AuthService`, cliente WebSocket). Se empieza a poblar en la Etapa 2 — Autenticación.
