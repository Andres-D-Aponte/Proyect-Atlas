# Shared

Componentes, pipes y directivas de UI reutilizables entre dominios (botones, tablas, modales, layout). Sin lógica de negocio propia. Se puebla a medida que surgen necesidades reales compartidas entre features (evitar crear abstracciones anticipadas sin un segundo caso de uso real).
