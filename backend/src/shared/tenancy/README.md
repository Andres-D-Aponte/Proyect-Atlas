# Multi-tenancy

Pendiente de implementación (Etapa 3 — Platform Owner, cuando exista `Company`). Contendrá el `TenantContext`, el `TenantGuard` y el middleware de Prisma que inyecta automáticamente el filtro `companyId`.

Ver `docs/02_Architecture.md` sección 5.
