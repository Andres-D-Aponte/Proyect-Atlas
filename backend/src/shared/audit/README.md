# Auditoría

Pendiente de implementación (a partir de Etapa 3 — Platform Owner, empezando por la impersonación). Interceptor + decorador `@Audit()` transversal, registros inmutables (solo inserción).

Ver `docs/02_Architecture.md` sección 11.
